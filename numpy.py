import numpy as np

# Creating arrays
a = np.array([1, 2, 3])
b = np.array([[1, 2], [3, 4]])

print(a.ndim)  # number of dimensions
print(a.shape) # shape (rows, cols)
print(a.size)  # total elements
print(a.dtype) # data type
